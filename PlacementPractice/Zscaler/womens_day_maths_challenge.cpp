#include <iostream>
#include <vector>
#include <stack>

using namespace std;

int helper(vector<int>&nums, stack<int>st,){

}

int findMinimumLength(vector<int> &arr) {
    stack<int> s;
    
    
    
    // return size of stack
    return s.size();
    
}

int main() {
    vector<int> arr;
    // arr.push_back(1);
    // arr.push_back(1);
    // arr.push_back(2);
    // arr.push_back(3);
    arr.push_back(2);
    arr.push_back(2);
    arr.push_back(2);
    arr.push_back(2);
    
    int minLength = findMinimumLength(arr);
    cout << minLength << endl;
    
    return 0;
}
