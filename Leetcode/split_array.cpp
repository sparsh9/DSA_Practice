#include<iostream>
#include<string>
#include<vector>
using namespace std;

bool isPossible(vector<int>& nums) {
        
    }

int main()
{
    vector<int> nums= [1,2,3,3,4,5];
    if(isPossible(nums))
        cout<<"true";
    else
        cout<<"false";
    return 0;
}